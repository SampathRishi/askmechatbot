# --- HTTP + crawling ---
httpx>=0.27,<1.0
beautifulsoup4>=4.12
lxml>=5.2
trafilatura>=1.12
pypdf>=4.3
playwright>=1.47          # optional JS-render fallback (run: playwright install chromium)

# --- Embeddings + vector store ---
sentence-transformers>=3.0
chromadb>=0.5.5

# --- LLM answer engine ---
anthropic>=0.40

# --- API backend ---
fastapi>=0.115
uvicorn[standard]>=0.30

# --- utilities ---
tenacity>=8.5             # retry/backoff at network boundaries
python-dotenv>=1.0        # load ANTHROPIC_API_KEY from a .env file
